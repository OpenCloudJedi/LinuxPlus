---
- name: Configure centos server
  hosts: centos
  become: yes

  tasks:
    - name: Add lab files to vagrant user's homedir
      synchronize:
        src: Materials/       
        dest: /home/vagrant/Materials
